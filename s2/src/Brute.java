import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Brute {
    public Brute(Point[] points) {
        // Constructor for initializing the Brute class
    }

    public int numberOfSegments() {
        // Implement this method to return the number of line segments found
        return 0;
    }

    public Iterable<Iterable<Point>> segments() {
        List<List<Point>> lineSegments = new ArrayList<>();

        // Implement this method to find and return all line segments

        return lineSegments;
    }

    public static void main(String[] args) {
        // Read input points and create an array of Point objects
        // Call the Brute constructor with the Point array

        // Initialize the Brute object

        // Print the number of line segments

        // Print the line segments
    }
}
